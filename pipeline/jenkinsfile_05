@Library('JenkinsLib')
import log

pipeline {
    agent any
    stages {
        stage('testlog') {
            steps {
               echo 'Test groovy script custom log'
               script {
                  log.info '****CUSTOM LOG****'
                  log.warning '****CUSTOM WARNING****'
               }  
            }
        }
    }
}